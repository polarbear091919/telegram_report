# E2E 실험 결과 — 2026-09-15

## 결론

Supabase 복원 후 **소량의 실제 데이터로 수집 → 파일/DB 저장 → 태깅 → 요약 저장 → 브라우저 표시까지 통과**했다. 이미지 PDF는 자동 분석하지 못하고 수동 검토로 분기한다. 대량 백필·장시간 안정성 검증은 이번 실험에 포함하지 않았다.

## 환경과 사전 검사

- 기존 `.venv` 사용. `pip check`: 의존성 충돌 없음.
- `python -m pytest tests langgraph_tagger -q --disable-warnings --tb=short`: **325 passed, 1 warning**.
- 태깅 모델: `gpt-5.4-nano`. 요약 모델: `gpt-5.4-mini`.
- LLM 동시성: 2. 태깅은 이번에 수집한 ID만 지정했다.
- 최초 DB 접근은 REST 521, SQL tenant/user not found로 실패했다. 복원 도중 REST PGRST205도 관찰됐다. 사용자가 Supabase 복원을 진행했고 이후 기존 데이터 조회가 정상화됐다.
- 복원 직후 보고서 15,455건, 요약 24건. 마지막 수집 메시지 ID 125164. 수집 실패 재시도 큐 0건.

## 실제 수집과 태깅

기존 `collector.run`에 실제 TelegramClient의 순차 조회를 PDF 3건에서 종료하는 실험용 제한을 적용했다. 마지막 수집 지점 다음부터 수집했으며, 최신 메시지로 수집 지점을 건너뛰지 않았다. 운영 소스 변경은 없다.

| Telegram 메시지 | DB 보고서 ID | 자료 | 결과 |
|---|---:|---|---|
| 125204 | 15480 | 스카이라이프 / NH투자증권 | auto, high, 053210 |
| 125205 | 15482 | 서울바이오시스 IR | review_needed, first_page_unreadable |
| 125206 | 15481 | 삼성증권 / KB증권 | auto, high, 016360 |

- 실제 수집 성공 3건, 비PDF 건너뜀 38건, 실패 0건.
- 저장 파일 3건의 크기와 SHA-256이 DB 값과 일치했다.
- 태깅 실행: auto 2건, review_needed 1건, 일시 오류·시간 초과·미처리 예외 모두 0건.
- 저장된 버전: `langgraph-tagger@2.0`, `KRX@2026-05-09`.
- 125204를 실제 재다운로드·저장해도 보고서 수는 증가하지 않았으며 기존 auto 태그가 유지됐다.
- 수동 검토 대상 15482가 검토 큐에 존재하며 PDF 3페이지 렌더링도 성공했다.

## 요약과 화면

- 실제 `analyze_stock`을 삼성증권(016360), 2026-05-11 이후로 실행했다. 대상은 새 보고서 15481 한 건이었다.
- PDF 전체 8페이지에서 요약 생성 후 `report_summaries` 저장·재조회 성공.
- 저장 결과: 목표주가 133,000 → 165,000원, 매수 유지, 긍정/위험 요인과 근거 페이지 포함. 이는 문서 추출 결과이며 투자 판단 평가는 하지 않았다.
- 요약 호출 사용량: 입력 11,825, 출력 238 토큰. 태깅 및 별도 점검 호출은 이 숫자에 포함하지 않는다.
- 같은 분석을 다시 실행했을 때 추출 단계 호출 없이 기존 요약을 재사용했다.
- 이전 보고서의 저장된 요약을 찾지 못해 `prev_match_type='none'`으로 마감했다. 과거 요약과의 실제 LLM 비교 분기는 이번 실험에서 검증하지 않았다.
- 복원 후 Streamlit AppTest로 분석·검토 앱 실행: 예외, 오류, 경고 모두 없음.
- 실제 브라우저에서 삼성증권 → LLM 분석 → 6개월을 선택해 `신규 분석 0건 · 캐시 1건`과 저장된 요약 카드를 확인했다.
- 분석 서버는 로컬 `http://127.0.0.1:8511`에서 실행했다.

## 최신 자료 별도 점검과 한계

- Telegram 인증 및 최신 20개 메시지 조회 성공. 그중 PDF 19건.
- 최신 메시지 136053(11페이지), 136051(8페이지)을 별도 임시 경로로 다운로드했다. 두 파일은 모든 페이지의 추출 텍스트가 0자였고 페이지마다 이미지가 있었다.
- 현재 태거는 두 파일 모두 `first_page_unreadable`로 처리한다. OCR이 구현돼 있지 않아 이미지 PDF의 자동 태깅·요약은 불가능하다.
- 최신 두 파일은 운영 DB에 추가하지 않았다. 운영 수집 지점을 뛰어넘지 않기 위해서다.
- 기존 텍스트 PDF로 실제 AI 태깅 2건 및 SK하이닉스 요약 호출도 별도 점검했다. 이 별도 점검 결과는 DB에 기록하지 않았다.
- 최근 90일/3개월 필터에서는 5월 자료가 보이지 않는다. 6개월 또는 1년으로 변경하면 조회된다.
- KRX CSV는 2,558종목이며 실행 시 버전은 `KRX@2026-05-09`였다. CLAUDE.md의 `KRX@2026-05-08` 설명과 다르다. 이번 실험에서는 기준표를 갱신하지 않았다.

## 최종 변경 상태

- 운영 보고서: **15,458건** (+3).
- 저장 요약: **25건** (+1).
- 수동 검토 큐: **421건** (+1).
- 마지막 수집 메시지 ID: **125206**.
- 수집 실패 큐: **0건**.
- 운영 코드 및 `.env` 변경 없음. 기존 사용자의 변경 사항 유지.
- 세부 실험 산출물은 `.pytest_cache/e2e/`에 저장했다. 테스트 캐시 정리 시 삭제될 수 있으므로 핵심 결과는 이 문서에 기록한다.

## 다음 작업

1. 이미지 PDF에 대한 OCR 또는 비전 입력 경로를 추가하고 최신 자료로 다시 검증.
2. 중단 기간의 자료 수집·태깅은 별도 백필 작업으로 수행. 이번 실험은 소량 검증이며 전체 밀린 자료를 처리하지 않았다.
